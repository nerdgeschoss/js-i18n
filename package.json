{
  "name": "@nerdgeschoss/i18n",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index",
  "scripts": {
    "build": "rimraf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"src/**/*.{tsx,ts,scss,json}\"",
    "lint": "tslint -c tslint.json 'src/**/*.{ts,tsx}' && prettier --list-different \"src/**/*.{tsx,ts,scss}\""
  },
  "author": "Jens Ravens <jens@nerdgeschoss.de> (https://jensravens.com/)",
  "license": "MIT",
  "devDependencies": {
    "@nerdgeschoss/config": "^0.0.1",
    "@types/node": "^10.11.4",
    "prettier": "^1.14.3",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "typescript": "^3.1.1"
  },
  "dependencies": {
    "moment": "^2.22.2"
  }
}
